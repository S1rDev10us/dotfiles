[package]
name = "REPLACE_ME"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14.0", features = ["dynamic_linking", "wayland"] }


[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
